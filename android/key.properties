# Android release keystore configuration
# Generated on: August 8, 2025 (Updated)
# 
# IMPORTANT: Keep this file secure and do not commit passwords to version control
# Consider using environment variables or secure CI/CD secrets for production

storeFile=../keystores/release-keystore.jks
storePassword=321123
keyAlias=release
keyPassword=321123

# Keystore Details:
# - Algorithm: RSA
# - Key Size: 2048 bits
# - Validity: 10,000 days (~27 years)
# - Distinguished Name: CN=Raj Chakraborty, OU=1, O=exampleKreativ, L=Dreaming, ST=Apocrypha, C=66

# Usage in android/app/build.gradle.kts:
# 
# val keystoreProperties = Properties()
# val keystorePropertiesFile = rootProject.file("key.properties")
# if (keystorePropertiesFile.exists()) {
#     keystorePropertiesFile.inputStream().use { keystoreProperties.load(it) }
# }
# 
# android {
#     signingConfigs {
#         create("release") {
#             keyAlias = keystoreProperties["keyAlias"] as String
#             keyPassword = keystoreProperties["keyPassword"] as String
#             storeFile = file(keystoreProperties["storeFile"] as String)
#             storePassword = keystoreProperties["storePassword"] as String
#         }
#     }
#     buildTypes {
#         release {
#             signingConfig = signingConfigs.getByName("release")
#         }
#     }
# }
